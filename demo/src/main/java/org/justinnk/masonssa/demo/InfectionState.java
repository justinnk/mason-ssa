/*
 * This repository might have licensing issues, which are in the process of being resolved, so no use is permitted at this time.
 */

package org.justinnk.masonssa.demo;

public enum InfectionState {
  SUSCEPTIBLE,
  INFECTIOUS,
  RECOVERED
}
